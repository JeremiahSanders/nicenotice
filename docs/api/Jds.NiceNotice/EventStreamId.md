# EventStreamId structure

Represents a unique identifier for an event stream. This type is used to uniquely identify and manage event streams, ensuring type safety when working with specific streams in the application's event notification system.

```csharp
public struct EventStreamId : IEquatable<EventStreamId>
```

## Public Members

| name | description |
| --- | --- |
| static [From](EventStreamId/From.md)(…) | Static constructor for creating a new event stream id from a string value. |
| override [ToString](EventStreamId/ToString.md)() | Returns the string value of this [`EventStreamId`](./EventStreamId.md). |
| [explicit operator](EventStreamId/op_Explicit.md) | Converts a string *value* to an [`EventStreamId`](./EventStreamId.md). |
| [implicit operator](EventStreamId/op_Implicit.md) | Returns the string value of an [`EventStreamId`](./EventStreamId.md). |

## Remarks

Event stream ids are a thin wrapper around a string intended to identify a single conceptual stream of messages. One can think of an event stream as the "door" or "path" the message follows. Individual applications determine how many distinct event streams their messages require.

Some applications have a single output notification stream for all messages. This is the place to start; all applications will need at least one.

Other applications route messages to distinct "paths" or "channels" based upon where they need to go or who needs to react to them. For example, they may have a stream for communicating metric-style notifications, or a stream for communicating state changes of managed data (e.g., in response to CRUD operations), or maybe even a stream specifically for dispatching error notifications.

For a further example, Console applications inherently have two conceptual notification streams provided by the operating system: standard out (`stdout`) and standard error (`stderr`). (This is only a comparison; there are no predefined console event stream ids.)

## See Also

* namespace [Jds.NiceNotice](../NiceNotice.md)
* [EventStreamId.cs](https://github.com/JeremiahSanders/nicenotice/tree/main/src/library/EventStreamId.cs)

<!-- DO NOT EDIT: generated by xmldocmd for NiceNotice.dll -->
